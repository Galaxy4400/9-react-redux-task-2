export const selectOrderColumn = (state) => state.app.orderColumn;

export const selectTasks = (state) => state.app.tasks;

export const selectLoadingStatus = (state) => state.status.loading;

export const selectCreatingStatus = (state) => state.status.creating;

export const selectDeletingStatus = (state) => state.status.deleting;

export const selectUpdatingStatus = (state) => state.status.updating;

export const selectSearchingStatus = (state) => state.status.searching;

